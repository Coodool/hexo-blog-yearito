component: website
name: hexo-blog
app: yearito
stage: dev

inputs:
  src:
    src: ./public # Upload static files generated by HEXO
    index: index.html
    # dist: ./dist
    # hook: npm run build
    # websitePath: ./
  region: ap-guangzhou
  bucketName: yearito-serverless-app
  protocol: http
  hosts:
    - host: yearito.cn
